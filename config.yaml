symbols:
  - BTC/USDT

data:
  timeframe: 5m
  lookback: 250

providers:
  openai:
    enabled: true
    model: gpt-4o-mini
    temperature: 0.15
    max_tokens: 400
  anthropic:
    enabled: true
    model: claude-3-5-sonnet-20240620
    temperature: 0.15
    max_tokens: 400
  grok:
    enabled: false
    model: grok-latest
    temperature: 0.15
    max_tokens: 400

ensemble:
  min_confidence: 0.6
  require_agreement: 2

risk:
  risk_per_trade: 0.01
  daily_loss_limit: 0.05
  kill_switch: true

exchange:
  name: mexc
  params: {}
  testnet: true

webhook_secret:
